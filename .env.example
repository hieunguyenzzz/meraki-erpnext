# ERPNext Configuration
ERPNEXT_VERSION=v15
SITE_NAME=erp.merakiwp.com
ADMIN_PASSWORD=your_admin_password
DB_PASSWORD=your_db_password

# ERPNext API (generate after site creation)
ERPNEXT_URL=http://100.65.0.28:8082
ERPNEXT_API_KEY=your_api_key
ERPNEXT_API_SECRET=your_api_secret

# Source PostgreSQL (Meraki NocoDB)
MERAKI_PG_HOST=14.225.210.164
MERAKI_PG_PORT=5432
MERAKI_PG_USER=meraki_noco_usr
MERAKI_PG_PASSWORD=your_pg_password
MERAKI_PG_DATABASE=meraki_nocodb

# Email Processing (Zoho IMAP)
# Used by email-processor and webhook services for email backfill/staging
ZOHO_IMAP_HOST=imappro.zoho.com
ZOHO_EMAIL=info@merakiweddingplanner.com
ZOHO_PASSWORD=your_zoho_app_password

# MinIO (Email Attachments)
# Used for storing email attachments
MINIO_ENDPOINT=minio-api.hieunguyen.dev
MINIO_ACCESS_KEY=your_minio_access_key
MINIO_SECRET_KEY=your_minio_secret_key
MINIO_BUCKET=merakiweddingplanner

# Email Staging Database (v1 - legacy)
# PostgreSQL for staging emails before processing
STAGING_DB_PASSWORD=meraki_staging

# Email Storage Database (v2 - new system)
# Separate PostgreSQL container for webhook_v2
EMAIL_STORAGE_PASSWORD=email_processor_secret

# Gemini AI (for email classification)
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.0-flash

# SendGrid (for test notification emails)
SENDGRID_API_KEY=your_sendgrid_api_key
